import React from 'react';
import { StyleSheet } from './StyleSheet';
import { Text, View, TouchableOpacity } from 'react-native';

const Button = ({ styleProps, children }) => {
  return (
    <TouchableOpacity>
      <View>
        <Text></Text>
      </View>
    </TouchableOpacity>
  );
};

export default Button;

const styles = StyleSheet.create({});
