import React from 'react';
import { Text, View, TouchableOpacity } from 'react-native';
import { StyleSheet } from './StyleSheet';

export const Button = ({ styleProps, children }) => (
  <TouchableOpacity>
    <View>
      <Text />
    </View>
  </TouchableOpacity>
);

const styles = StyleSheet.create({});
