import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const OpenCommentsModalIcon = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default OpenCommentsModalIcon;

const styles = StyleSheet.create({});
