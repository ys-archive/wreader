import { selectors as selAuth } from '../reducers/auth';
import { selectors as selData } from '../reducers/data';
import { selectors as selImage } from '../reducers/image';
import { selectors as selSwiper } from '../reducers/swiper';

export { selAuth, selData, selImage, selSwiper };
