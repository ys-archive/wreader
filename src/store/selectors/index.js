import { selectors as selAuth } from "../reducers/auth";
import { selectors as selComments } from "../reducers/comments";
import { selectors as selData } from "../reducers/data";
import { selectors as selDataFetch } from "../reducers/data.fetch";
import { selectors as selImage } from "../reducers/image";
import { selectors as selSwiper } from "../reducers/swiper";
import { selectors as selSort } from "../reducers/sort";

export {
  selAuth,
  selComments,
  selData,
  selDataFetch,
  selImage,
  selSwiper,
  selSort,
};
