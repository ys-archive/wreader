class Category {
  constructor(id, title, chapterLimit, maxLength) {
    this._id = id;
    this._title = title;
    this._chapterLimit = chapterLimit;
    this._maxLength = maxLength;
  }
}

export default Category;
